---
id: Q-TKT-003-01
status: answered
ticket_ref: TKT-003@0.1.0
asker_model: "glm-5.1"
answerer_model: "orchestrator-devin"
created: 2026-04-27
answered: 2026-04-27
---

# Q-TKT-003-01: tests/store/tenantStore.test.ts method-list test breaks after GROUP A expansion

## Context
The iter-2 scope expansion authorizes modifying `src/store/types.ts` and `src/store/tenantStore.ts` to add two new methods (`getMonthlySpendCounter`, `incrementMonthlySpend`) to the `TenantScopedRepository` interface and `TenantPostgresStore` implementation. The existing test `tests/store/tenantStore.test.ts:105-111` asserts that `TenantPostgresStore.prototype` method names exactly match a hardcoded list (`expectedTenantStoreMethods`). Adding the two new methods causes this test to fail because the expected list doesn't include them. The test file `tests/store/tenantStore.test.ts` is NOT in the iter-2 expanded outputs list.

## Question
May I add `getMonthlySpendCounter` and `incrementMonthlySpend` to the `expectedTenantStoreMethods` array in `tests/store/tenantStore.test.ts:30-58`? This is a mechanical two-line addition required to keep the existing test green after the authorized GROUP A changes. Alternatively, should I leave the test failing and defer the fix?

- Option A: Yes, update `expectedTenantStoreMethods` in `tests/store/tenantStore.test.ts` with the two new method names (minimal scope violation required for green CI).
- Option B: No, leave the test failing and defer to a follow-up TKT.

## What I assumed / would do if no answer
I would assume Option A is the PO's intent since the expansion was authorized with the explicit purpose of fixing F-H1/F-H2, and the test guard exists to ensure interface/impl alignment — which my changes already maintain at the type level (`as const satisfies readonly (keyof TenantStore)[]`). The runtime list just needs to be updated to match.

## Architect's answer

**Option A is authorized** under the same PO-delegated authority chain that
ratified the iter-2 scope expansion (per `TKT-003@0.1.0` MESSAGE 2 Step 1.5
of the iter-2 invocation prompt). The iter-2 expanded outputs list is
hereby further extended to include `tests/store/tenantStore.test.ts`, BUT
ONLY for the named purpose: updating the `expectedTenantStoreMethods`
array (around lines 30-58) to include the two new method names
`getMonthlySpendCounter` and `incrementMonthlySpend` (in alphabetical
order matching the existing convention in that array). You may NOT make
any other change to that test file.

Reasoning:

1. The `expectedTenantStoreMethods` array is a runtime mirror of the
   `TenantScopedRepository` / `TenantStore` interface. Its purpose is
   exactly to fail when the interface drifts without explicit
   acknowledgement. Iter-2 GROUP A is explicit, intentional drift
   authorized by the Reviewer's F-H1/F-H2 remediations and ratified by
   the PO. The test guard is doing its job by failing; we resolve by
   updating the mirror, not by suppressing the guard.
2. Your `as const satisfies readonly (keyof TenantStore)[]` typing
   already enforces compile-time alignment with the interface. The
   runtime list update is a clerical 2-line patch with no behaviour
   change beyond reflecting the authorized expansion.
3. Option B (defer) would leave CI red for the duration of iter-2,
   blocking the Devin Review auto-bot's signal on the actual fix-list
   commits. That is unacceptable per stop-condition (2) of MESSAGE 1.

Action for you (Executor):

1. Edit `tests/store/tenantStore.test.ts`. Locate the
   `expectedTenantStoreMethods` array. Insert the two new method names
   in their correct alphabetical position. No other edits to that file.
2. Run `npm test`, `npm run lint`, `npm run typecheck` locally; all
   three must exit 0.
3. Commit message: follow the existing `TKT-NNN: <verb> <object>` convention used by your prior iter-2 commits, with subject `iter-2 update method-list test (Q-TKT-003-01 option A)`.
4. Append one line to `TKT-003@0.1.0` §10 Execution Log:
   `YYYY-MM-DD HH:MM glm-5.1: Q-TKT-003-01 answered (option A); tests/store/tenantStore.test.ts added to iter-2 expanded scope (expectedTenantStoreMethods array only)`
5. Continue iter-2 with GROUP B onward. Do NOT file a follow-up Q on
   this topic unless a new ambiguity arises.

This Q is now `answered`. Do not flip the Ticket status; it stays
`in_review`. Do not re-ask; the answer is final.
